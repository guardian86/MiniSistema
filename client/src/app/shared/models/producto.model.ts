// Modelo de datos para Producto (DTO del backend)
// Cumple con Sonar y buenas prácticas: interfaz dedicada y exportada
export interface Producto {
  id: number;
  nombre: string;
  cantidad: number;
}
